#ifndef GLIBC_FIRST_VERSIONS_H_
#define GLIBC_FIRST_VERSIONS_H_

/* We need this file, but because we are building for static linkage, it is
 * generated. So we have an empty one here (symbol versions have no meaning
 * with static linkage.
 */

#endif
